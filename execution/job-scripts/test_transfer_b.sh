#!/usr/bin/env bash
# test_transfer_b: with remap — tests whether AP absolute path remap works
echo "hostname: $(hostname)"
echo "date: $(date)"
echo "transfer test B — with remap" > result.txt
echo "done"
